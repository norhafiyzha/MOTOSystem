@model IEnumerable<MOTOSystem.Models.Class_Package>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cp_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cp_fees)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cp_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cp_fees)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cp_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.cp_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cp_id })
        </td>
    </tr>
}

</table>
